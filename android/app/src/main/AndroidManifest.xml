<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Removed USE_FINGERPRINT: deprecated at API 28 and superseded by
         USE_BIOMETRIC above; minSdk is 26 so it bought two API levels of
         nothing. -->
    <!-- Removed RECORD_AUDIO: its only consumer was VoiceInputService, which
         was left behind when the generative-AI feature was deleted and has no
         callers. A microphone permission on a health app is a Play review
         question you do not want to have to answer for dead code. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Removed WRITE_EXTERNAL_STORAGE entirely (a no-op from API 29, and this
         app never writes to external storage — every export goes to
         getTemporaryDirectory()/getApplicationDocumentsDirectory() and out via
         a scoped FileProvider share). READ is capped at the last API where it
         meant anything, for file_picker on older devices. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Removed USE_EXACT_ALARM: Play Console's acceptable-use-case policy for
         this auto-granted, non-revocable permission is limited to alarm/timer
         apps and calendar apps — a medication reminder app doesn't qualify and
         risks a Play review rejection. Verified safe to drop: every exact-timed
         alarm in this app (grep for `alarmClock:` in background_alarm_service.dart)
         is scheduled via AndroidAlarmManager's alarmClock:true (setAlarmClock()),
         which Android does not gate behind EITHER exact-alarm permission —
         SCHEDULE_EXACT_ALARM above is kept declared (harmless, user-grantable,
         not policy-restricted) for any future setExact() use, but neither
         permission is actually read by the current alarm-scheduling code. -->

    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Removed FOREGROUND_SERVICE_SPECIAL_USE: no service declares the
         specialUse foreground type + PROPERTY_SPECIAL_USE_FGS_SUBTYPE, so on
         targetSdk 34+ it triggers a Play Console rejection. The alarm service
         (android_alarm_manager_plus) is a JobService (BIND_JOB_SERVICE), not a
         foreground special-use service, so this permission was never needed. -->

    <!-- Steps + Sleep: live pedometer + Health Connect reads -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_SLEEP"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_DISTANCE"/>
    <!-- Wearable biometrics. Requested as a SEPARATE tier from the biometrics
         screens, never in the first-run steps+sleep sheet: Health Connect is
         all-or-nothing per request and throttles re-prompting, so bundling
         these would make the base grant likelier to be refused. See
         HealthDataService.biometricTypesFor.

         Names that are not what you would guess from the Dart type:
         HRV is READ_HEART_RATE_VARIABILITY (no _RMSSD suffix) and SpO2 is
         READ_OXYGEN_SATURATION (not READ_BLOOD_OXYGEN).

         No READ_VO2_MAX: `health` 13.3.2 declares no VO2MAX type on either
         platform, so nothing could read it. health_data_types_test.dart fails
         when that changes. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY"/>
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION"/>
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE"/>
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE"/>
    <!-- Feature-gated in Health Connect; HealthDataService probes
         isSkinTemperatureAvailable() and drops the type from the request when
         the installed version is too old, because one unsupported type makes
         Android refuse the whole sheet. -->
    <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE"/>
    <uses-permission android:name="android.permission.health.READ_EXERCISE"/>

    <!-- Vitals: opt-in write-back of BP/glucose/weight readings the user
         already logged locally (Settings toggle, off by default). READ is
         requested alongside WRITE only because the plugin has no
         write-only request. -->
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE"/>
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_PRESSURE"/>
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE"/>
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_GLUCOSE"/>
    <uses-permission android:name="android.permission.health.READ_WEIGHT"/>
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT"/>

    <application
        android:label="Dlyminder"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- showWhenLocked / turnScreenOn / showOnLockScreen were declared
                 here. This is the app's ONLY activity — /alarm is a Flutter
                 route inside it — so those attributes made the ENTIRE app
                 visible over the device lock screen, permanently. After an
                 alarm fired, anyone could dismiss it and browse medicines,
                 vitals, diary and period data without entering the device PIN.
                 They are now set at runtime, only while an alarm is on screen.
                 See MainActivity.applyLockScreenFlags. -->
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Health Connect permissions rationale (required for review) -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        <!-- Android 14+ Health Connect permission-usage screen -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        
        <!-- AdMob App ID.
             TODO(ads): replace with your REAL AdMob Android application ID from
             the AdMob console before production. This is Google's public TEST
             app ID (safe for development only). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        
        <!-- CRITICAL: ActionBroadcastReceiver for notification action buttons (Snooze/Dismiss) -->
        <receiver 
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
        
        <!-- Android Alarm Manager Plus for reliable background alarms -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false"/>
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false"/>
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Steps + Sleep home-screen widget (native half of home_widget feature).
             Dart side: lib/core/services/home_widget_service.dart -->
        <receiver
            android:name=".StepsSleepWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/steps_sleep_widget_info"/>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Health Connect app (for the `health` plugin) -->
        <package android:name="com.google.android.apps.healthdata"/>
    </queries>
</manifest>
